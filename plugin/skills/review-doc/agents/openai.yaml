interface:
  display_name: "AndThen - Review Doc"
  short_description: "Specialist document review"
  default_prompt: "Review a spec, PRD, plan, or other document for completeness, clarity, edge cases, and technical accuracy. Use this when the target is clearly a document rather than a general review request. Document: "

policy:
  allow_implicit_invocation: true
