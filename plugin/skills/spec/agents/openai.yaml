interface:
  display_name: "AndThen - Spec"
  short_description: "Generate a new spec or FIS file before implementation; runs fresh-context doc self-review and blocks `spec-ready` on unresolved decision Notes"
  default_prompt: "Generate a complete Feature Implementation Specification – analyze the codebase first, write the FIS using the template, then run fresh-context doc self-review. Pass `--visual` to hand the produced FIS to the `andthen:visualize` skill after self-review. Feature: "

policy:
  allow_implicit_invocation: true
