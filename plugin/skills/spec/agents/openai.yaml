interface:
  display_name: "AndThen - Spec"
  short_description: "Generate a new spec or FIS file before implementation; `--visual` delegates the produced FIS to the `andthen:visualize` skill"
  default_prompt: "Generate a complete Feature Implementation Specification – analyze the codebase first, then write the FIS using the template. Pass `--visual` to hand the produced FIS to the `andthen:visualize` skill after saving. Feature: "

policy:
  allow_implicit_invocation: true
