interface:
  display_name: "AndThen - Ops"
  short_description: "Update state, story status, and FIS checkboxes"
  default_prompt: "Execute deterministic operation: update the project's `State` document as defined in the **Project Document Index**, update plan status, update FIS checkboxes, or create a standardized commit. Operation: "

policy:
  allow_implicit_invocation: true
