interface:
  display_name: "AndThen - Ops"
  short_description: "State management, git conventions, progress tracking"
  default_prompt: "Execute deterministic operation: update STATE.md, plan status, FIS checkboxes, or create standardized commit. Operation: "

policy:
  allow_implicit_invocation: true
