interface:
  display_name: "AndThen - Execute Plan (Agent Teams)"
  short_description: "Execute plan via Agent Teams pipeline"
  default_prompt: "Execute all stories in the implementation plan through the Agent Teams pipeline: parallel spec generation, then exec-spec and review-gap per story. Plan directory: "

policy:
  allow_implicit_invocation: true
