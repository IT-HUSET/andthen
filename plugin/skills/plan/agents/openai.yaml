interface:
  display_name: "AndThen - Plan"
  short_description: "Create an implementation plan with FIS specs for every story"
  default_prompt: "Create an implementation plan with story breakdown and FIS specs for every story. Requires an existing prd.md in the input directory. Plan directory: "

policy:
  allow_implicit_invocation: true
