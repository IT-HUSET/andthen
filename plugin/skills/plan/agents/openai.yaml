interface:
  display_name: "AndThen - Plan"
  short_description: "Create an implementation plan with FIS specs for every story; `--visual` delegates to the andthen:visualize skill"
  default_prompt: "Create an implementation plan with story breakdown and FIS specs for every story from a local PRD directory, `--issue <N>`, or a GitHub issue URL. Pass `--visual` to hand the produced local `plan.json` bundle to the andthen:visualize skill after validation. PRD source or plan directory: "

policy:
  allow_implicit_invocation: true
